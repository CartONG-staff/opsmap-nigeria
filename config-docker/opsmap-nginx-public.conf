worker_processes 1;
error_log stderr warn;
pid /run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include mime.types;
    default_type application/octet-stream;

    # Define custom log format to include reponse times
    log_format main_timed '$remote_addr - $remote_user [$time_local] "$request" '
                          '$status $body_bytes_sent "$http_referer" '
                          '"$http_user_agent" "$http_x_forwarded_for" '
                          '$request_time $upstream_response_time $pipe $upstream_cache_status';

    access_log /dev/stdout main_timed;
    error_log /dev/stderr notice;

    keepalive_timeout 65;

    # Write temporary files to /tmp so they can be created as a non-privileged user
    client_body_temp_path /tmp/client_temp;
    proxy_temp_path /tmp/proxy_temp_path;
    fastcgi_temp_path /tmp/fastcgi_temp;
    uwsgi_temp_path /tmp/uwsgi_temp;
    scgi_temp_path /tmp/scgi_temp;

    # #########################################################################
    # Welcome page
    # #########################################################################

    server {
        listen 8891;

        root /var/www/html/;

        location / {
            try_files $uri $uri/ @country welcome/index.html;
        }

        location @country {
            rewrite ^/([a-z]+)/(.*)$ /$1/$2 break;
            rewrite ^/([a-z]+)$ /$1/index.html break;
            index index.html;
        }

    }

    # #########################################################################
    # Brazil server definition
    # #########################################################################

    server {
        listen 8892;
        root /var/www/html/brazil;
    }

    # #########################################################################
    # Bulgaria server definition
    # #########################################################################

    # server {
    #     listen 8893;
    #     root /var/www/html/bulgaria;
    # }

    # #########################################################################
    # Bulgaria server definition
    # #########################################################################

    server {
        listen 8894;
        root /var/www/html/burkina-faso;
    }

    # #########################################################################
    # Bulgaria server definition
    # #########################################################################

    server {
        listen 8895;
        root /var/www/html/ethiopia;
    }
    # #########################################################################
    # Moldova server definition
    # #########################################################################

    server {
        listen 8896;
        root /var/www/html/moldova;
    }

    # #########################################################################
    # Nigeria server definition
    # #########################################################################

    server {
        listen 8897;
        root /var/www/html/nigeria;
    }

    # #########################################################################
    # Romania server definition
    # #########################################################################

    server {
        listen 8898;
        root /var/www/html/romania;
    }

    # #########################################################################
    # Showcase server definition
    # #########################################################################

    server {
        listen 8899;
        root /var/www/html/showcase;
    }

    # #########################################################################
    # Slovakia server definition
    # #########################################################################

    server {
        listen 8900;
        root /var/www/html/slovakia;
    }

    # #########################################################################
    # Somalia server definition
    # #########################################################################

    server {
        listen 8901;
        root /var/www/html/somalia;
    }

    # #########################################################################
    # Sudan server definition
    # #########################################################################

    server {
        listen 8902;
        root /var/www/html/sudan;
    }

    # #########################################################################
    # server definition
    # #########################################################################

    # 8903 -> ukraine (dedicated config file)

    # #########################################################################
    # Yemen server definition
    # #########################################################################

    server {
        listen 8904;
        root /var/www/html/yemen;
    }

    # #########################################################################
    # gzip configuration
    # #########################################################################

    gzip on;
    gzip_proxied any;
    gzip_types text/plain application/xml text/css text/js text/xml application/x-javascript text/javascript application/json application/xml+rss;
    gzip_vary on;
    gzip_disable "msie6";

    # Include other server configs
    include /etc/nginx/conf.d/*.conf;
}
