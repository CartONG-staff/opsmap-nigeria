version: '3.3'

services:
  opsmaps:
    container_name: opsmaps
    environment:
      - VUE_APP_RIDL_TOKEN=*
    build:
      context: .
      args:
        opsmaps: "brazil burkina-faso ethiopia moldova nigeria romania showcase slovakia somalia sudan ukraine yemen"
    ports:
      - 8080:8080
